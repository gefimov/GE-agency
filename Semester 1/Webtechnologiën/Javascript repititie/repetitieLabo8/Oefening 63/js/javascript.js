function showText(text, id){
    let element = document.getElementById(id);
    element.textContent = text;
}

let randomNumber = 6, number;

while (randomNumber != number){
     number = prompt("geef een getal tss 1 en 10");
    }
